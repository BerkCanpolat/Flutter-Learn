import 'package:flutter/material.dart';

class RoboPadding {
  static const homeGeneral = EdgeInsets.symmetric(horizontal: 15);
  static const homeImage = EdgeInsets.only(top: 200);
  static const homeTitle = EdgeInsets.only(top: 50);
  static const homeButton = EdgeInsets.only(bottom: 50);
  static const signImage = EdgeInsets.only(top: 10);
  static const signHeadTitlePadding = EdgeInsets.only(top: 25, bottom: 25);
  static const signOrTextPadding = EdgeInsets.only(left: 20, right: 20);
  static const signButtonPasswordPadding = EdgeInsets.only(top: 20, bottom: 55);
  static const createButtonSignUp = EdgeInsets.only(top: 30, bottom: 20);
  static const createButtonRowGeneral = EdgeInsets.only(top: 30,bottom: 50);
}